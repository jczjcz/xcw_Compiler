int n;
int arr[10];
int bubblesort()
{
    int i;
    int j;
    if (arr[1] > arr[2]) {
        // swap(&arr[j], &arr[j+1]); 
        int tmp;
        tmp = arr[j+1];
        arr[j+1] = arr[j];
        arr[j] = 2;
    }
    return 0;
}